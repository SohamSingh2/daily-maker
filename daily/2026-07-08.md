# Sort Daily Wins Log

Date: 2026-07-08

Tiny app concept: a warm command center **daily wins log**.

Core touches:

- localStorage persistence
- export as plain text
- keyboard shortcuts

Palette:

- Background: `#202c39`
- Accent: `#f29559`
- Text: `#ead2ac`

Starter file:

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Sort Daily Wins Log</title>
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      background: #202c39;
      color: #ead2ac;
      font-family: system-ui, sans-serif;
    }
    main {
      width: min(34rem, calc(100% - 2rem));
      border: 1px solid #f29559;
      border-radius: 8px;
      padding: 1.25rem;
    }
    button {
      border: 0;
      border-radius: 6px;
      padding: 0.7rem 0.9rem;
      background: #f29559;
      color: #202c39;
      font-weight: 700;
    }
  </style>
</head>
<body>
  <main>
    <h1>Sort Daily Wins Log</h1>
    <p>A warm command center daily wins log with localStorage persistence, export as plain text, and keyboard shortcuts.</p>
    <button id="spark">Make today count</button>
    <p id="out"></p>
  </main>
  <script>
    const notes = ["Start tiny.", "Ship the useful bit.", "Make it pleasant."];
    spark.onclick = () => out.textContent = notes[Math.floor(Math.random() * notes.length)];
  </script>
</body>
</html>
```

Build note: keep the first version under one screen and make the primary action obvious.
