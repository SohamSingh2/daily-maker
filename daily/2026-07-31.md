# Track Mood Tracker

Date: 2026-07-31

Tiny app concept: a midnight dashboard **mood tracker**.

Core touches:

- progress rings
- random prompt generation
- localStorage persistence

Palette:

- Background: `#262322`
- Accent: `#ddc9b4`
- Text: `#f0f7ee`

Starter file:

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Track Mood Tracker</title>
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      background: #262322;
      color: #f0f7ee;
      font-family: system-ui, sans-serif;
    }
    main {
      width: min(34rem, calc(100% - 2rem));
      border: 1px solid #ddc9b4;
      border-radius: 8px;
      padding: 1.25rem;
    }
    button {
      border: 0;
      border-radius: 6px;
      padding: 0.7rem 0.9rem;
      background: #ddc9b4;
      color: #262322;
      font-weight: 700;
    }
  </style>
</head>
<body>
  <main>
    <h1>Track Mood Tracker</h1>
    <p>A midnight dashboard mood tracker with progress rings, random prompt generation, and localStorage persistence.</p>
    <button id="spark">Make today count</button>
    <p id="out"></p>
  </main>
  <script>
    const notes = ["Start tiny.", "Ship the useful bit.", "Make it pleasant."];
    spark.onclick = () => out.textContent = notes[Math.floor(Math.random() * notes.length)];
  </script>
</body>
</html>
```

Build note: keep the first version under one screen and make the primary action obvious.
