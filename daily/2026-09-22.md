# Collect Quote Shuffler

Date: 2026-09-22

Tiny app concept: a soft arcade **quote shuffler**.

Core touches:

- keyboard shortcuts
- editable cards
- compact mobile layout

Palette:

- Background: `#17252a`
- Accent: `#3aafa9`
- Text: `#def2f1`

Starter file:

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Collect Quote Shuffler</title>
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      background: #17252a;
      color: #def2f1;
      font-family: system-ui, sans-serif;
    }
    main {
      width: min(34rem, calc(100% - 2rem));
      border: 1px solid #3aafa9;
      border-radius: 8px;
      padding: 1.25rem;
    }
    button {
      border: 0;
      border-radius: 6px;
      padding: 0.7rem 0.9rem;
      background: #3aafa9;
      color: #17252a;
      font-weight: 700;
    }
  </style>
</head>
<body>
  <main>
    <h1>Collect Quote Shuffler</h1>
    <p>A soft arcade quote shuffler with keyboard shortcuts, editable cards, and compact mobile layout.</p>
    <button id="spark">Make today count</button>
    <p id="out"></p>
  </main>
  <script>
    const notes = ["Start tiny.", "Ship the useful bit.", "Make it pleasant."];
    spark.onclick = () => out.textContent = notes[Math.floor(Math.random() * notes.length)];
  </script>
</body>
</html>
```

Build note: keep the first version under one screen and make the primary action obvious.
