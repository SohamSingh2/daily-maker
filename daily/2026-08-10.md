# Collect Mood Tracker

Date: 2026-08-10

Tiny app concept: a paper notebook **mood tracker**.

Core touches:

- editable cards
- daily streak tracking
- export as plain text

Palette:

- Background: `#0f1a20`
- Accent: `#c2f970`
- Text: `#ffffff`

Starter file:

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Collect Mood Tracker</title>
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      background: #0f1a20;
      color: #ffffff;
      font-family: system-ui, sans-serif;
    }
    main {
      width: min(34rem, calc(100% - 2rem));
      border: 1px solid #c2f970;
      border-radius: 8px;
      padding: 1.25rem;
    }
    button {
      border: 0;
      border-radius: 6px;
      padding: 0.7rem 0.9rem;
      background: #c2f970;
      color: #0f1a20;
      font-weight: 700;
    }
  </style>
</head>
<body>
  <main>
    <h1>Collect Mood Tracker</h1>
    <p>A paper notebook mood tracker with editable cards, daily streak tracking, and export as plain text.</p>
    <button id="spark">Make today count</button>
    <p id="out"></p>
  </main>
  <script>
    const notes = ["Start tiny.", "Ship the useful bit.", "Make it pleasant."];
    spark.onclick = () => out.textContent = notes[Math.floor(Math.random() * notes.length)];
  </script>
</body>
</html>
```

Build note: keep the first version under one screen and make the primary action obvious.
