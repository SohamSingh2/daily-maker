# Sort Focus Timer

Date: 2026-09-01

Tiny app concept: a quiet and crisp **focus timer**.

Core touches:

- localStorage persistence
- one-click reset
- export as plain text

Palette:

- Background: `#101820`
- Accent: `#f2aa4c`
- Text: `#f7f7f2`

Starter file:

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Sort Focus Timer</title>
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      background: #101820;
      color: #f7f7f2;
      font-family: system-ui, sans-serif;
    }
    main {
      width: min(34rem, calc(100% - 2rem));
      border: 1px solid #f2aa4c;
      border-radius: 8px;
      padding: 1.25rem;
    }
    button {
      border: 0;
      border-radius: 6px;
      padding: 0.7rem 0.9rem;
      background: #f2aa4c;
      color: #101820;
      font-weight: 700;
    }
  </style>
</head>
<body>
  <main>
    <h1>Sort Focus Timer</h1>
    <p>A quiet and crisp focus timer with localStorage persistence, one-click reset, and export as plain text.</p>
    <button id="spark">Make today count</button>
    <p id="out"></p>
  </main>
  <script>
    const notes = ["Start tiny.", "Ship the useful bit.", "Make it pleasant."];
    spark.onclick = () => out.textContent = notes[Math.floor(Math.random() * notes.length)];
  </script>
</body>
</html>
```

Build note: keep the first version under one screen and make the primary action obvious.
