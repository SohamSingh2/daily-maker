# Track Micro Journal

Date: 2026-07-12

Tiny app concept: a soft arcade **micro journal**.

Core touches:

- progress rings
- editable cards
- export as plain text

Palette:

- Background: `#2d3047`
- Accent: `#fffd82`
- Text: `#ff9b71`

Starter file:

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Track Micro Journal</title>
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      background: #2d3047;
      color: #ff9b71;
      font-family: system-ui, sans-serif;
    }
    main {
      width: min(34rem, calc(100% - 2rem));
      border: 1px solid #fffd82;
      border-radius: 8px;
      padding: 1.25rem;
    }
    button {
      border: 0;
      border-radius: 6px;
      padding: 0.7rem 0.9rem;
      background: #fffd82;
      color: #2d3047;
      font-weight: 700;
    }
  </style>
</head>
<body>
  <main>
    <h1>Track Micro Journal</h1>
    <p>A soft arcade micro journal with progress rings, editable cards, and export as plain text.</p>
    <button id="spark">Make today count</button>
    <p id="out"></p>
  </main>
  <script>
    const notes = ["Start tiny.", "Ship the useful bit.", "Make it pleasant."];
    spark.onclick = () => out.textContent = notes[Math.floor(Math.random() * notes.length)];
  </script>
</body>
</html>
```

Build note: keep the first version under one screen and make the primary action obvious.
