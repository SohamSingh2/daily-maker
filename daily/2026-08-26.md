# Sort Stretch Reminder

Date: 2026-08-26

Tiny app concept: a clean studio **stretch reminder**.

Core touches:

- random prompt generation
- progress rings
- one-click reset

Palette:

- Background: `#17252a`
- Accent: `#3aafa9`
- Text: `#def2f1`

Starter file:

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Sort Stretch Reminder</title>
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      background: #17252a;
      color: #def2f1;
      font-family: system-ui, sans-serif;
    }
    main {
      width: min(34rem, calc(100% - 2rem));
      border: 1px solid #3aafa9;
      border-radius: 8px;
      padding: 1.25rem;
    }
    button {
      border: 0;
      border-radius: 6px;
      padding: 0.7rem 0.9rem;
      background: #3aafa9;
      color: #17252a;
      font-weight: 700;
    }
  </style>
</head>
<body>
  <main>
    <h1>Sort Stretch Reminder</h1>
    <p>A clean studio stretch reminder with random prompt generation, progress rings, and one-click reset.</p>
    <button id="spark">Make today count</button>
    <p id="out"></p>
  </main>
  <script>
    const notes = ["Start tiny.", "Ship the useful bit.", "Make it pleasant."];
    spark.onclick = () => out.textContent = notes[Math.floor(Math.random() * notes.length)];
  </script>
</body>
</html>
```

Build note: keep the first version under one screen and make the primary action obvious.
