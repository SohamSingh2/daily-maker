# Stack Tiny Budget Board

Date: 2026-09-03

Tiny app concept: a midnight dashboard **tiny budget board**.

Core touches:

- keyboard shortcuts
- one-click reset
- export as plain text

Palette:

- Background: `#0b132b`
- Accent: `#5bc0be`
- Text: `#f4f1de`

Starter file:

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Stack Tiny Budget Board</title>
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      background: #0b132b;
      color: #f4f1de;
      font-family: system-ui, sans-serif;
    }
    main {
      width: min(34rem, calc(100% - 2rem));
      border: 1px solid #5bc0be;
      border-radius: 8px;
      padding: 1.25rem;
    }
    button {
      border: 0;
      border-radius: 6px;
      padding: 0.7rem 0.9rem;
      background: #5bc0be;
      color: #0b132b;
      font-weight: 700;
    }
  </style>
</head>
<body>
  <main>
    <h1>Stack Tiny Budget Board</h1>
    <p>A midnight dashboard tiny budget board with keyboard shortcuts, one-click reset, and export as plain text.</p>
    <button id="spark">Make today count</button>
    <p id="out"></p>
  </main>
  <script>
    const notes = ["Start tiny.", "Ship the useful bit.", "Make it pleasant."];
    spark.onclick = () => out.textContent = notes[Math.floor(Math.random() * notes.length)];
  </script>
</body>
</html>
```

Build note: keep the first version under one screen and make the primary action obvious.
